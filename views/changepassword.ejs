<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="csrf-token" content="<%= csrfToken %>"/> 
        
        <script src="https://cdn.tailwindcss.com"></script>
        <title>Change password</title>
    </head>
    <body>
        <div class="grid grid-cols-6">
            <div class="col-start-3 col-span-2">
            
                <h1>Change password</h1>
                <form action="/changepassword" method="post">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="py-3">
                        <label for="currentpassword">Current Password</label>
                        <input type="text" name="currentpassword" placeholder="currentpassword" required class= "border border-gray-300 text-gray-900 rounded p-2 w-full">
                    </div>
                    <div class="py-3">
                        <label for="newpassword">New Password</label>
                        <input type="text" name="newpassword" required placeholder="newpassword" class= "border border-gray-300 text-gray-900 rounded p-2 w-full">
                    </div>
                    <div class="py-3">
                        <label for="confirmpassword">Confirm Password</label>
                        <input type="password" name="confirmpassword" required placeholder="confirmpassword" class= "border border-gray-300 text-gray-900 rounded p-2 w-full">
                    </div>
                   
                    <div class="py-3">
                        <button type="submit"class="text-white bg-green-600 hover:bg-green-800 font-meduim rounded px-5 py-2">Confirm</button>
                    </div>
                </form>

            </div>
        </div>
    </body>
</html>